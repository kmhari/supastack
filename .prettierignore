node_modules/
dist/
build/
.next/
coverage/
pnpm-lock.yaml
package-lock.json
yarn.lock

# Vendored upstream — never our format
infra/supabase-template/
apps/web/src/theme/

# Speckit + Claude config + planning docs — don't reformat
.specify/
.claude/
specs/
plan.md
CLAUDE.md
*.md.bak

# CI workflows YAML — be conservative
.github/

# CI-only prettier disagreement (tracked in #30). Identical prettier@3.8.3 +
# config + bytes pass locally on macOS arm64 AND Linux x64 Docker, but the
# GitHub Actions runner consistently flags this one file. Reproducible only
# on the hosted runner. Skip until root-caused.
apps/api/tests/unit/secret-store-name.test.ts
