# CLAUDE.md

@AGENTS.md

- Deep working context (feature history, deploy targets, VM runbooks) lives in
  `develop/` — gitignored, local-only. Check `develop/CLAUDE.md` if it exists.
- Use `pnpm` (never npm/yarn). Run `pnpm lint` and `pnpm test` before
  declaring work done.
